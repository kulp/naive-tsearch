#cmakedefine01 HAS_INCLUDE_SEARCH_H

#cmakedefine01 HAS_ENUM_VISIT

#cmakedefine01 HAS_FUNCTION_TSEARCH

#cmakedefine01 HAS_FUNCTION_TFIND

#cmakedefine01 HAS_FUNCTION_TDELETE

#cmakedefine01 HAS_FUNCTION_TWALK

#cmakedefine01 HAS_FUNCTION_TDESTROY

#if HAS_INCLUDE_SEARCH_H
#include <search.h>
#endif

#if !HAS_ENUM_VISIT
typedef enum { preorder, postorder, endorder, leaf } VISIT;
#endif

#if !HAS_FUNCTION_TSEARCH
NAIVE_TSEARCH_API
void *tsearch(const void *key, void **rootp,
    int (*compar)(const void *, const void *));
#endif

#if !HAS_FUNCTION_TFIND
NAIVE_TSEARCH_API
void *tfind(const void *key, void *const *rootp,
    int (*compar)(const void *, const void *));
#endif

#if !HAS_FUNCTION_TDELETE
NAIVE_TSEARCH_API
void *tdelete(const void *key, void **rootp,
    int (*compar)(const void *, const void *));
#endif

#if !HAS_FUNCTION_TWALK
NAIVE_TSEARCH_API
void twalk(const void *root,
   void (*action)(const void *nodep, VISIT which, int depth));
#endif

#if !HAS_FUNCTION_TDESTROY
void tdestroy(void *root, void (*free_node)(void *nodep));
#endif

